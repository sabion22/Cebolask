rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Regra simples para equipe interna (qualquer um logado pode ler/escrever tudo)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
